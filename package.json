{
  "name": "echemai-research-notes",
  "version": "1.0.0",
  "private": true,
  "description": "EchemAI Research Notes — selected electrochemistry + AI research.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "check": "node scripts/check.mjs",
    "build": "npm run check && node scripts/build.mjs",
    "preview": "node scripts/serve.mjs dist"
  },
  "engines": {
    "node": ">=20"
  }
}
